apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-webapp
  labels:
    app: ui-webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui-webapp
  template:
    metadata:
      name: ui_webapp
      labels:
        app: ui-webapp
    spec:
      containers:
        - name: ui-webapp
          image: aravindepic/ui_webapp:v1
          imagePullPolicy: IfNotPresent
          ports:
            - name: ui-port
              containerPort: 8080


---

apiVersion: v1
kind: Service
metadata:
  name: ui-webapp
spec:
  selector:
    app: ui-webapp
  ports:
     - name: ui-port
       port: 8080
       targetPort: ui-port
  type: NodePort
    

      